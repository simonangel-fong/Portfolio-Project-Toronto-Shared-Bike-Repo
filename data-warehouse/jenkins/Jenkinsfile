pipeline {
  agent any

  triggers {
    // cron '0 0 * * *'    // every midnight
    githubPush() // Triggers on GitHub push events
  }

  stages {

    stage('test') {
      steps {
        dir("data-warehouse/postgresql"){
          echo "#################### test ####################"
          sh '''
          ls -l
          '''
        }
      }
    }
  }

}