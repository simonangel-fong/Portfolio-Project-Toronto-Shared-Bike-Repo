pipeline {
  agent any

   options {
    // limit only one instance
    // disableConcurrentBuilds()
    // show timestamp for event
    

    stages{
      stage("test"){
        steps{
          sh '''
            pwd ls
          '''
        }
      }
    }
  }
}
 