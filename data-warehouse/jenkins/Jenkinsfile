pipeline {
    agent {
        docker { image 'postgres:15' }
    }
    stages {
        stage('Test') {
            steps {
                sh '''
                    ping -c4 8.8.8.8
                    pwd
                    hostname
                '''
            }
        }
    }
}