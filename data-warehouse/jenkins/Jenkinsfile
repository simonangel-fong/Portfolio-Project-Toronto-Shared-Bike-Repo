pipeline {
    agent {
        docker { image 'postgres:15' }
    }
    stages {
        stage('Test') {
            steps {
                sh '''
                    pwd
                    hostname
                '''
            }
        }
    }
}